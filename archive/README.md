# Archive

Content not currently wired into `index.html`, kept here instead of deleted:

- `data/notes/` and `data/notes.json` — the "Field Notes" write-ups (technical
  lab-log style entries). Removed from the live page in the minimal redesign;
  bring back if there's enough real content to justify a dedicated section.
- `data/life/` and `data/life.json` — the "Beyond Engineering" hobby entries
  (climbing, art, photography, food). Condensed to one sentence in the About
  section on the live page.
- `data/projects.json` — an older flat project-list format, superseded by the
  one-file-per-project structure in `data/projects/`. Already unused before
  the redesign.
